input {
    tcp {
        port => 5621
        codec => "json"
    }
}

output {
    elasticsearch {
        hosts => "192.168.0.254"
        index => "%{[appname]}-%{+YYYY.MM.dd}"
    }
    stdout {
        codec => rubydebug { }
    }
}